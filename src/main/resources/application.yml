spring:
  application:
    name: cuentas-challenge

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.challenge.cuentas: DEBUG
